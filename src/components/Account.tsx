import React from 'react';

import { containerStyle } from "./New";

export default function Account() {
    return (
        <div style={containerStyle}>
            ACCOUNT PAGE
        </div>
    );
}